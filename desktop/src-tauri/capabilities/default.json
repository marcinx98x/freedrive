{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and preferences windows",
  "windows": ["main", "preferences"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "opener:default",
    "dialog:default",
    "shell:default",
    "autostart:default"
  ]
}
